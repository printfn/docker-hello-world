# tiny hello world

`01-rust` contains a small Hello World program in Rust (size: 13360 bytes).

`02-asm` uses nasm and ld to achieve a size of 4360 bytes.

`03-asm` uses nasm to manually create an ELF file, using 157 bytes.


View disassembly using `objdump -b binary -m i386:x86-64 -D a.out`.

